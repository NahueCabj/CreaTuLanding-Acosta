const BotonMultiuso = (props) => {

const styles = {
    backgroundColor: props.color,
    color: 'white',
    border: 'none'
}    
    return(
        <button>Click!</button>
    )
}

export default BotonMultiuso